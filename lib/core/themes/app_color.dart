import 'dart:ui';

class AppColor {
  AppColor._();

  // Light Theme
  static const lightPrimary = Color(0xFF2C2E35);
  static const lightBackground = Color(0xFFF5F5F5);
  static const lightTextPrimary = Color(0xFF000000);
  static const lightTextSecondary = Color(0xFFFFFFFF);

  // Dark Theme
  static const darkPrimary = Color(0xFFF5F5F5);
  static const darkBackground = Color(0xFF2C2E35);
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFF000000);
  static const darkTextPlaceholder = Color.fromARGB(255, 171, 170, 170);

  // Common Colors
  static const textPlaceholder = Color(0xFF696969);
  static const placeholder = Color(0xFFD3D3D3);
  static const error = Color(0xFFFF3B30);
}
